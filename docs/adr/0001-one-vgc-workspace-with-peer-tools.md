# One VGC workspace with peer tools

We model Unburden as one VGC workspace with peer tools rather than separate apps or separate contexts because the Damage Calculator and Speed Benchmark share Pokémon, set, regulation, meta, and modifier language. Their prompt grammars and result models differ, but the ubiquitous language should stay shared so future tools can reuse the same concepts without fragmenting the product.
