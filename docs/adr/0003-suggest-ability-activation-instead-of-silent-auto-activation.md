# Suggest ability activation instead of silent auto-activation

Selecting an ability does not silently create field conditions or activation state. The workspace may suggest relevant activation, such as adding weather for a weather-based ability or `unburden-active` for Unburden, but the user must accept it before it affects results because explicit activation keeps calculations trustworthy and avoids hidden battle assumptions.
